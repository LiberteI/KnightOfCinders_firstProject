using UnityEngine;

public class EnemySuperArmourManager : MonoBehaviour
{
    /* 
        give enemy a super armour build-up meter.
        when enemy receive a hit, the meter builds up, but it will decrease with time.

        when the meter reaches the threshold. super armour icon appears, and the sprite glow.
    */
}
